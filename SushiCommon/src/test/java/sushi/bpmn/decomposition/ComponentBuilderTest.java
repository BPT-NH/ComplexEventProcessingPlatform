package sushi.bpmn.decomposition;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

import sushi.bpmn.element.BPMNProcess;

public class ComponentBuilderTest extends AbstractDecompositionTest {
	
//TODO an micha: drüber nachdenken und löschen
	@Test
	public void testComponentBuilder(){
//		ComponentBuilder componentBuilder = new ComponentBuilder(process);
//		BPMNProcess foldedProcess = componentBuilder.buildComponents();
//		assertTrue("Should be 4, but was " + foldedProcess.getBPMNElementsWithOutSequenceFlows().size(),
//				foldedProcess.getBPMNElementsWithOutSequenceFlows().size() == 4);
//		assertTrue(foldedProcess.getBPMNElementsWithOutSequenceFlows().contains(startEvent));
//		assertTrue(foldedProcess.getBPMNElementsWithOutSequenceFlows().contains(endEvent));
	}
	
}
